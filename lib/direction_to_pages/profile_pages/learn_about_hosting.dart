import 'package:flutter/material.dart';

class LearnAboutHosting extends StatelessWidget {
  const LearnAboutHosting({super.key});


  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('LearnAboutHosting '),
      ),
      body: const Center(
        child: Text('LearnAboutHosting page!'),
      ),
    );
  }
}