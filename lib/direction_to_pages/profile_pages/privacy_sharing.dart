import 'package:flutter/material.dart';

class PrivacyNSharing extends StatelessWidget {
  const PrivacyNSharing({super.key});


  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Privacy and Sharing '),
      ),
      body: const Center(
        child: Text('hello sir!'),
      ),
    );
  }
}